#!/usr/bin/env bash
set -eu -o pipefail

chown -R mysql:mysql /var/lib/mysql /var/run/mysqld
service mysql start

while [[ $(mysqladmin ping --silent) != "mysqld is alive" ]]; do
    printf .
    sleep 1
done
